{% set name = "refnx" %}
{% set version = "0.0.17" %}
{% set sha256 = "7db7513d7def2e75e0760076561798a546b19623ed42e2cdb86910cc5cf54641" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/refnx/refnx/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - python
    - numpy
    - cython
    - setuptools

  run:
    - python
    - {{ pin_compatible('numpy') }}
    - scipy
    - xlrd
    - pandas
    - uncertainties
    - h5py
    - matplotlib
    - ipywidgets
    - traitlets
    - six

test:
  requires:
    - pytest
  commands:
    - py.test --pyargs refnx -v --duration=0

  imports:
    - refnx

about:
  home: https://github.com/refnx/refnx
  license: BSD 3-Clause
  license_file: LICENSE
  summary: 'Neutron and X-ray Reflectometry Analysis'
  doc_url: https://refnx.github.io

extra:
  recipe-maintainers:
    - andyfaff
