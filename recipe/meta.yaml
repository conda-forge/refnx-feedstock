{% set name = "refnx" %}
{% set version = "0.1.11" %}
{% set sha256 = "eeb4a910a07037c25ee0dbbeb5f7b2e44294c4e61e04f12d68c295cd735f2fda" %}


package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/refnx/refnx/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0

  skip: true  # [py27]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ cdt('mesa-libgl-devel') }}  # [linux]
    - {{ cdt('mesa-dri-drivers') }}  # [linux]
    - {{ cdt('libselinux') }}  # [linux]
    - {{ cdt('libxdamage') }}  # [linux]
    - {{ cdt('libxxf86vm') }}  # [linux]
    - llvm-openmp  # [osx]

  host:
    - python
    - numpy
    - cython
    - setuptools
    - xorg-libxfixes  # [linux]
    - llvm-openmp  # [osx]

  run:
    - python
    - {{ pin_compatible('numpy') }}
    - scipy
    - llvm-openmp  # [osx]

test:
  requires:
    - pytest
    - pytest-qt
    - nose
    - uncertainties
    - scipy>=1.2.0
    - xlrd
    - pandas
    - h5py
    - matplotlib
    - ipywidgets
    - traitlets
    - corner
    - tqdm
    # try using pyqt from defaults instead of conda-forge
    - pyqt >=5  # [osx and win]
    - periodictable

  commands:
    - export MPLBACKEND=agg  # [osx and linux]
    - py.test --pyargs refnx -v --durations=0  # [osx and win]
    # can't get the pyqt GUI test to run for some reason, not even with xvfb
    # - xvfb-run -a bash -c 'py.test --pyargs refnx -v --duration=0'  # [linux]
    - conda install -y -c defaults pyqt; xvfb-run -w 3 -d bash -c 'py.test --pyargs refnx -v --durations=0'  # [linux]

  imports:
    - refnx

about:
  home: https://github.com/refnx/refnx
  license: BSD 3-Clause
  license_file: LICENSE
  summary: 'Neutron and X-ray Reflectometry Analysis'
  doc_url: https://refnx.github.io

extra:
  recipe-maintainers:
    - andyfaff
