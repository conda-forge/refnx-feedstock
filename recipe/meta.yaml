{%set name = "refnx" %}
{%set version = "0.0.10" %}
{%set sha256 = "0408e0101e0adf38d6f7064c95c741980b0c002e356dbee34312a14518c3b0d7" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/refnx/refnx/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - toolchain
    - python
    - numpy x.x
    - cython
    - setuptools

  run:
    - python
    - numpy x.x
    - scipy
    - xlrd
    - pandas
    - lmfit
    - emcee
    - uncertainties
    - h5py
    - six

test:
  requires:
    - pytest

  imports:
    - refnx

about:
  home: https://github.com/refnx/refnx
  license: BSD 3-Clause
  license_file: LICENSE
  summary: 'Neutron and X-ray Reflectometry Analysis'
  doc_url: https://refnx.github.io

extra:
  recipe-maintainers:
    - andyfaff
